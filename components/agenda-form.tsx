'use client'
import {useState} from 'react'
import {createAgendaEvent} from '@/app/admin/agenda/actions'
import SubmitButton from '@/components/submit-button'
export default function AgendaForm({profiles}:{profiles:{id:string;full_name:string|null;role:string}[]}){const[scope,setScope]=useState('General');return <form action={createAgendaEvent} className="form-card"><div className="panel-heading"><div><span className="eyebrow">Nueva actividad</span><h3>Agregar a agenda</h3></div></div><div className="form-grid"><label className="span-2">Título<input name="title" required/></label><label>Tipo<select name="event_type" defaultValue="Tarea"><option>Tarea</option><option>Cita con cliente</option><option>Seguimiento</option><option>Llamada</option><option>Pago</option><option>Cita gubernamental</option><option>Otro</option></select></label><label>Asignación<select name="assignment_scope" value={scope} onChange={e=>setScope(e.target.value)}><option>General</option><option value="Específico">Específico</option></select></label>{scope==='Específico'?<label className="span-2">Responsable<select name="assigned_to" required defaultValue=""><option value="" disabled>Selecciona</option>{profiles.map(p=><option key={p.id} value={p.id}>{p.full_name||'Usuario'} · {p.role}</option>)}</select></label>:null}<label>Inicia<input name="starts_at" type="datetime-local" required/></label><label>Termina<input name="ends_at" type="datetime-local"/></label></div><label>Descripción<textarea name="description" rows={3}/></label><SubmitButton pendingText="Guardando…">Guardar actividad</SubmitButton></form>}
